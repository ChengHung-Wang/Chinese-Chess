#include <iostream>
#include <string>
#include "GameManager.h"

using namespace std;

int main() {
	GameManager gm = GameManager();
	std::string opt;
	while (std::cin >> opt) {
		if (opt == "save") {

		}
		else if (opt == "setFile") {
            cout << "as1234" << endl;
		}
		else if (opt == "setNew") {

		}
		else if (opt == "getRound") {

		}
		else if (opt == "getMove") {

		}
		else if (opt == "move") {

		}
		else if (opt == "getTime") {

		}
	}
}